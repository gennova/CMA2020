<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mio.miocma2020">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".up1.Qualification"></activity>
        <activity android:name=".up1.Reception" />
        <activity android:name=".up1.ProcessingOne" />
        <activity android:name=".LoginStaff" />
        <activity android:name=".transaction.BarcodeTrans" />
        <activity android:name=".transaction.AddTransaction" />
        <activity android:name=".Transactions" />
        <activity android:name=".Farmers" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>